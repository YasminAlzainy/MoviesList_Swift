# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'MoviesList_Swift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MoviesList_Swift
    pod 'SDWebImage', '~> 4.0'
    pod 'Alamofire', '~> 4.0.0'
    # pod 'SwiftyJSON', '~> 4.0'

  target 'MoviesList_SwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MoviesList_SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
